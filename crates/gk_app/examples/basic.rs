use gk_app::AppBuilder;
use gk_core::GKWindowManager;
use gk_winit::{runner, Manager, Window};

fn main() {
    // AppBuilder::init_with(|| Ok(()))
    //     .set_runner(runner)
    //     .add_plugin(Manager::new())
    //     .run()
    //     .unwrap();
}
